name: Workflow Tests

on:
  push:
    branches: ["main"]
  merge_group:
    types: [checks_requested]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  workflow-tests:
    runs-on: ubuntu-latest
    env:
      GT_CLOUD_API_KEY: ${{ secrets.GT_CLOUD_API_KEY }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12"]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Checkout griptape-nodes-library-testing
        uses: actions/checkout@v4
        with:
          repository: griptape-ai/griptape-nodes-library-testing
          path: griptape-nodes-library-testing

      - name: Move testing library to sibling directory
        run: mv griptape-nodes-library-testing ../griptape-nodes-library-testing

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Install dependencies
        run: uv pip install --system griptape-nodes pytest pytest-asyncio python-dotenv

      - name: Run workflow tests
        run: pytest -s tests/workflows/test_integration_workflows.py
